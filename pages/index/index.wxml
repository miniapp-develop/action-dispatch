<view class="container">
    <view class="sample_card" data-url="/pages/pageB/index" bind:tap="onTap">
        点击打开其他页面(自定义)
    </view>
    <view class="sample_card" data-url="mini://page?path=%2Fpages%2FpageB%2Findex" bind:tap="onTap">
        点击打开其他页面(默认)
    </view>
    <view class="sample_card" data-url="mini://miniapp?appId=xxxxxx" bind:tap="onTap">
        点击打开其他小程序
    </view>
    <view class="sample_card" data-url="mini://webview?url=xxxxxx&title=this_is_title&color=red" bind:tap="onTap">
        点击打开网页(方式一)
    </view>
    <view class="sample_card" data-url="https://aa.bb.cc/xyz?key=value" bind:tap="onTap">
        点击打开网页(方式二)
    </view>
    <view class="sample_card" data-url="mini://dialog?title=Demo&content=Hello" bind:tap="onTap">
        点击弹出对话框(自定义)
    </view>
    <view class="sample_card" data-url="mini://func/aFn?title=Demo&content=Hello" bind:tap="onTap">
        点击弹出对话框(默认)
    </view>
</view>
